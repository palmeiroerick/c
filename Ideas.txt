- Add buffering to c_putchar
- Create a c_getchar function, using the read syscall
- Compare the assembly code of the different versions of the letter function
- Explore bitwise operations and implementation-defined behavior
- Study the different C I/O functions

- Find out a better way of doing the comb exercise
- Different approaches to solve comb2:
    - The one I first used: Use two while loops and simulate a number with two digits
    using chars (when the char representing the last digit is greater than '9' I
    increment the char representing the first digit by 1, and reset the last to '0');
    - Four while loops (one for each digit, d1, d2, d3, d4);
    - Use two ints to represent the numbers instead of four chars;
    - One loop + explicit state transition (as in the first comb exercise);
    - Generate a combination by index.
- How to verify if two numbers being represented as a sequence of chars (each char
being one digit) are equal, greater than, or less than each other.

- Make a putnbr that supports output in other numerical bases